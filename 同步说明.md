# 热点局域网同步：使用说明与实现报告

已在现有应用上增加同步模块，没有重写界面。Mac 仍使用 Swift / SwiftUI / SwiftData；Android 仍使用 Java 原生界面，本地存储由 AtomicFile JSON 迁移到 SQLite。

## 安装与首次配对

1. Mac 退出旧版应用，打开本目录 `dist/学习日程.app`。Android 1.3 使用了新发布签名；如已安装 1.2，先在旧版导出 JSON 备份，再卸载旧版、安装 `dist/学习日程-安卓版.apk` 并恢复备份。
2. 手机开启热点，Mac 连接这个热点。两端都打开学习日程 App。
3. Mac 进入“学习设置 → 热点局域网同步”，点击“读取当前网关”，核对这是手机热点的网关地址，再点击“生成配对二维码”。
4. 手机进入“设置 → 热点局域网同步”，点击“扫描 Mac 配对二维码”并允许摄像头权限。扫码成功后，手机开启最长 120 秒的配对窗口。
5. 回到 Mac 点击“扫码后配对并同步”。二维码只含一次性随机配对密钥和失效时间，不含课程、课表、任务、备份或长期凭据；无需复制任何配对内容。
6. 完成后两端显示状态，手机关闭监听，Mac 关闭网络会话。以后无需重复输入配对信息。

Android 1.3 扫码后在 App 前台开启最长两分钟的临时前台服务。切到后台不会立即关闭；手动关闭、划掉任务、同步成功或超时会关闭服务与通知，系统不会自动重启它。局域网不可达、手机端口被系统限制等情况会显示失败，不会覆盖成空数据。摄像头帧只在内存中解码，不保存、不上传。

## 自动同步与手动同步

自动同步每天最多成功一次，以北京时间自然日计算，日期持久化到本地数据库。Mac 在启动、回到前台时检查：当天未成功、距离上次自动尝试至少 30 分钟、当前网关与已配对地址相同。满足后才访问手机。不会定时重试，也不会在午夜唤醒应用。

Android 已配对后，打开或回到 App 时可开启一个短暂接收窗口；同日已完成自动同步或距离上次窗口不到 30 分钟时不自动开启。没有后台常驻服务、Wi-Fi 扫描、mDNS、Bonjour 或 WebSocket。

“立即同步”不受每日次数限制。手机按钮开启接收窗口，Mac 按钮执行同步；Mac 连续点击会排队，上一次会话失效并关闭后才开始下一次。每次执行仍需要手机有新的接收窗口，排队不能远程唤醒已经关闭的手机服务。

**因此，不能保证仅连接热点就必定同步。** Mac 的触发时机与手机接收窗口需要重合。若错过窗口，请先在手机点击“立即同步”，再在 Mac 点击“立即同步”。本版没有增加持续的网络变化监视器；Mac 一直在前台时重连热点，需要切回 App 或手动同步。

## 数据、协议与冲突

- 保留课程、固定事项、学习任务、完成记录、排程设置，沿用已有业务 UUID；新增生成任务使用确定性 ID，重排时优先保留既有同一时段任务的 ID。
- 每条同步记录包含 `kind / id / payload / deleted / version / device / sequence`。`payload` 为现有 Codable JSON 的 Base64，日期继续使用自 2001-01-01 UTC 起的秒数。
- 每台设备维护自己的递增序号、逻辑时钟和对端游标。只发送游标之后发生变化的记录，不同步数据库文件；重复变化不会再次产生新记录。
- 删除后保留完整同步删除标记。Android 直接保存为 SQLite 行；Mac 的 SwiftData 同步账本保存删除标记，业务界面不再显示已删除记录。
- 冲突比较逻辑版本，再用设备 ID 确定相同版本的赢家。这保证两端一致，不依赖两台设备的系统时间，但不等于严格按现实时间选择最后一次编辑。旧版数据没有修改时间，首次配对的既有冲突也按此确定性规则处理。
- Android 统一解决合并后的未来排程冲突，并消除旧随机 ID 与新稳定 ID 对应的完全相同时段。完成记录按任务归一化，由胜出的任务确认结果决定，避免重复计入进度。Mac 导入结果时不会立即再重排一次；普通本地排程行为保留。
- 业务数据、删除标记和游标同事务提交。两台设备之间不假装存在跨设备数据库事务；中断后允许重传，下一次继续收敛。

服务端为按需 HTTPS，默认监听 `0.0.0.0:8765`，接口如下：

| 接口 | 用途 |
|---|---|
| `POST /sync/pair` | 在限时窗口内用二维码的一次性随机密钥换取随机长期 token |
| `GET /sync/status` | 认证后读取协议与监听状态 |
| `POST /sync/changes` | 提交客户端变化，返回服务端增量与会话标识 |
| `POST /sync/finish` | 确认本地已提交，记录成功并关闭手机服务 |
| `POST /sync/cancel` | 认证后提前关闭本次窗口 |

Mac 使用系统安全随机数生成 256 位一次性密钥并编码进二维码。Android 扫码后只在本次两分钟窗口内接受该密钥；首次 HTTPS 配对成功时，Mac 记录实际手机证书的 SHA-256 指纹。其后每次都验证证书，并使用 Bearer token 认证。Mac token 保存在钥匙串，Android 只保存 token 的 SHA-256 摘要，TLS 私钥保存在 Android Keystore。重新配对会更换 token，旧凭据随即失效。当前只支持一组手机／Mac 配对。

TLS 实现参考 [Apple 手动验证服务器身份文档](https://developer.apple.com/documentation/Foundation/performing-manual-server-trust-authentication) 和 [Android TLS 密钥用途说明](https://developer.android.com/reference/kotlin/android/security/keystore/KeyProtection.Builder)。

## 网络释放与日志

每个 HTTP 响应声明关闭连接。成功确认后，Android 关闭监听端口、已接入 socket 和本次窗口的截止定时器，服务线程退出。Mac 等待 URLSession 失效回调后结束任务。失败时尽量发送取消请求；若连接已经断开，手机依靠独立的 120 秒总窗口上限关闭。单个 socket 读取另有 10 秒超时。

两端记录开始、目标设备、变化数量、新增／修改／删除数量、成功／失败和资源关闭。日志不记录 token、二维码密钥或日程内容。Mac 的文件日志位于 `~/Library/Application Support/StudyPlanner/sync.log`；Android 日志位于应用私有目录，可在设置中查看。

原应用用于日期切换和学习确认的界面定时器仍保留，它们不会触发同步网络轮询。测试检查的是同步线程和连接的结束，不是要求整个 App 停止所有本地活动。

## 迁移与限制

- Android 自动读取原 `planner-v1.json` 并事务迁移到 `planner-sync-v1.db`；原 JSON 保留，不再作为实时数据源。JSON 导出／恢复功能继续使用。恢复备份产生的删除和修改也会进入下一次同步。
- Mac 保留原 SwiftData 数据库与模型。首次普通保存同步账本前，写出 `before-local-sync.json` 业务数据备份。新增账本行使旧版 Mac 程序不再适合直接打开升级后的数据库，请使用新版。
- 当前单次请求／响应上限 16 MB，尚未实现分页。超过上限会失败；不会标记成功。删除标记暂不清理，以免长期离线设备使已删除数据复活。
- 热点网关改变时需重新读取地址并重新配对。不扫描整个网段。VPN 改变默认网关时，自动同步可能跳过。
- 尚未在你的实体手机热点上测试，也没有进行实体设备耗电测量；不同手机的热点隔离和后台限制仍需现场验证。

## 修改文件

| 范围 | 主要文件 |
|---|---|
| 同步模型与冲突 | 新增 `Sources/StudyCore/SyncLedger.swift`、`Android/src/local/studyplanner/SyncLedger.java` |
| 手机存储与网络 | 新增 `PlannerDatabase.java`、`SyncServer.java`、`PlannerRuntime.java`、`SyncWindow.java`、`SyncWindowService.java`、`QrScannerActivity.java` |
| Mac 网络客户端 | 新增 `Sources/StudySync/SyncClient.swift`、`Sources/StudyPlanner/PairingQRCodeView.swift`；更新 `Package.swift` |
| 业务与持久化 | 更新 `Models.swift`、`ScheduleEngine.swift`、`LocalRepository.swift`、`Planner.java` |
| 现有界面接入 | 更新 `PlannerStore.swift`、`MainView.swift`、`Editors.swift`、`MainActivity.java` |
| 构建与兼容 | 更新 `AndroidManifest.xml`、`Scripts/build-app.sh`、`Android/export-desktop.py` |
| 测试与文档 | 新增两端同步测试、设备 instrumentation 和 `Scripts/test-sync.sh`；更新两份 README |

## 验证结果与复测

已完成 Swift 单元／持久化测试、Java 原有排程回归、Swift 与 Java 协议互通、Android 15 ARM64 隔离模拟器上的 SQLite/TLS/服务关闭测试，以及 Mac 客户端直接连接该模拟器的完整 HTTPS 配对和双向同步。Mac 使用临时数据库与内存凭据，Android 使用独立模拟器；测试没有修改用户正式日程。

普通 Swift 套件包含二维码密钥、排程、持久化与同步协议测试，其中实际设备联调默认跳过；Java 回归覆盖大量生成场景与关键不变量。Android 设备测试另行覆盖扫码内容校验、SQLite、TLS、授权、增量同步和服务释放。

运行不依赖模拟器的回归：

```sh
Scripts/test-sync.sh
```

设备测试先构建 `Android/build.sh`、`Android/test-device-build.sh`，只在专用测试模拟器上安装主 APK 和 `Android/build/device-tests/tests.apk`，然后运行：

```sh
adb -s <测试模拟器> shell am instrument -w local.studyplanner.synctests/local.studyplanner.SyncInstrumentation
```

实体手机建议依次验证：两边离线分别新增、修改、删除；同步后比较记录；同日重启 Mac 不再自动同步；手动同步仍可执行；中途断开热点后重新开启窗口再同步；查看日志确认关闭状态。恢复备份前先导出当前数据。


## 1.2 连接故障修复

旧版会在手机 Activity 的 `onStop` 关闭端口，所以切换到其他 App 会让尚未到期的配对窗口一起失效。新版将窗口交给最长 120 秒的非粘性前台服务；保留服务端截止时间，并添加独立的服务总截止时间。没有后台自动重试或常驻监听。

手机未配对时，“立即同步”会提示先扫描 Mac 二维码；Mac 显示失败阶段、HTTP 状态或网络错误码。手机错误按配对窗口失效、二维码密钥错误、认证失败、会话失效、数据处理失败区分，关闭资源时不再把失败提示覆盖掉。

临时服务遵循 [Android 前台服务生命周期](https://developer.android.com/develop/background-work/services/fgs/launch) 和 [停止前台服务](https://developer.android.com/develop/background-work/services/fgs/stop-fgs) 的规则，不从开机广播启动，不自动恢复。

新增的生命周期回归已实际等待完整两分钟：验证切到后台后的配对与同步、完成后的关闭、120 秒后的服务销毁、通知移除、端口拒绝连接和同步线程退出，共通过 13 项检查。

## 1.3 二维码配对

首次配对改为 Mac 生成二维码、Android 扫描。安卓不再显示或复制“配对码.证书指纹”，Mac 也不再提供粘贴框。二维码设置了短时失效时间，扫码内容经过协议、版本、长度、字符集和有效期校验；错误二维码不会启动网络窗口。扫码依赖 ZXing Core 3.5.3，许可证与校验值记录在 `Android/lib/README.md`。

Android 1.3 同时会把 1.2 空白包中 7 个旧的默认时段标识稳定迁移为跨平台 UUID，避免 Java 本地可读但 Mac 同步解码失败。迁移不改变星期或可学习时间，并随同一个 SQLite 事务保存。
